### 🚀 v1.0.2-beta: 矿场部署极客版优化与自动防封更新

**✨ 核心更新 (Enhancements)**
* **定制客户端命名优化**：全线统一隧道定制客户端名称为无特征的简短名称 `go-xy` (Linux) 与 `go-xy.exe` (Windows)，提升部署时的隐蔽性。
* **一键自动防封部署/更新脚本增强**：Web 界面定制隧道时生成的 Linux 一键部署命令得到史诗级强化：
  - 新增 `killall -9 go-xy 2>/dev/null` 自动猎杀旧进程机制。
  - 新增 `rm -f go-xy tunnel.log` 自动清痕机制。
  - 完美实现“杀旧换新”一条龙服务：当旧服务器 IP 被封时，矿场运维只需在服务器上一键粘贴新命令，即可全自动实现旧防封通道销毁与新通道挂载。

**（注：此版本与核心抽水引擎逻辑完全向后兼容）**
