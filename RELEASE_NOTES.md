### 🚀 v1.0.1-beta: 隧道可视化体验与日志 API 热修复

**✨ 核心更新 (Hotfix & Improvements)**
* **隧道加密状态全链路可视化**：
  - 核心底层新增 `IsEncrypted` 标识透传。
  - Web 控制台矿机列表中，走专属定制客户端加密隧道的机器，其名称旁将永久显示专属安全护盾（🛡️）标识。
  - 系统控制台实时日志（System Logs）中，专属隧道流量在握手成功时会显式打印 `[隧道加密🛡️]` 的高亮日志头，方便快速区分直连与隧道流量。
* **日志系统热修复 (Bug Fix)**：修复了 Web 面板前后台 API 路由参数不匹配（`/api/logs` vs `/api/logs/tail`）导致的面板实时日志无法显示问题，现已完美输出滚动日志。
* **编译问题修复**：修复了底层加密组件的无用 UUID 包导入导致的源码交叉编译失败异常。

**（注：其余核心抽水逻辑与 Auto-Vardiff 引擎均继承自史诗级重构版 v1.0.0-beta）**
